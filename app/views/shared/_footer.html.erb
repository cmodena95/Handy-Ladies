<div class="footer">
  <div class="line-above"></div>
  <div class="container d-flex justify-content-between align-items-center" >
      <%= link_to '<i class="fas fa-home"></i>'.html_safe, root_path, class: "footer-link" %>
      <% if user_signed_in? %>
        <%= link_to '<i class="far fa-user-circle"></i>'.html_safe, worker_path(current_user.id), class: "footer-link-middle" %>
      <% end %>
      <%= link_to '<i class="far fa-comments"></i>'.html_safe, root_path, class: "footer-link" %>
  </div>
  <div class="rectangle"></div>
</div>
